server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri:  https://github.com/Prasanth-Thottempudi/config-repo
          clone-on-start: true
      label: main  # (branch name)


